/*
 * Given a trash and an object bins, sort this trash into the proper bin.
 * There are three types of trashs here: 'waste', 'recycling', 'compost'
 * The object bins will be in the following format:
 * {
        waste: 0,
        recycling: 0,
        compost: 0
    }
 * 
 * sample input:
 *  const bins = {
        waste: 4,
        recycling: 2,
        compost: 5
    }
    const trash = 'recycling'
 * sample output:
 *  {
        waste: 4,
        recycling: 3,
        compost: 5
    }
 */

/*
 * 与えられたゴミとゴミ箱のオブジェクトをもとにゴミを正しいゴミ箱に分別して捨てなさい。
 * ここでは3種類のゴミがあると仮定します。: 'waste', 'recycling', 'compost'
 * ゴミ箱オブジェクトは下記のようなフォーマットになっています。
 * {
        waste: 0,
        recycling: 0,
        compost: 0
    }
 * 
 * sample input:
 *  const bins = {
        waste: 4,
        recycling: 2,
        compost: 5
    }
    const trash = 'recycling'
 * sample output:
 *  {
        waste: 4,
        recycling: 3,
        compost: 5
    }
 */

const smartGarbage = (trash, bins) => {
    // Code here!

    // Remember to return an array!
}