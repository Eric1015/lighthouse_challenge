/*
 * Given an array of movements a marker will take, return the final position of the marker. The marker is initially placed at the origin ([0,0]).
 * Using the usual mathematical coordinates, the position will be an array with first element as x and second as y: ex. [x,y] = [0,0]
 * Each move is a string showing one of 'north', 'east', 'south', 'west'
 * 
 * sample input:
 *  const moves = ['north', 'north', 'west', 'west', 'north', 'east','north']
 * sample output:
 *  [-1, 4]
 */

/*
 * 与えられた動きの配列をもとに、マーカーが最終的にどの位置にいるのかを求めなさい。マーカーの最初の位置は原点（[0,0]）です。
 * 通常の数学の座標を使うことで、配列の最初の要素はｘを、２番目の要素はｙを表します。例えば、[x,y] = [0,0]
 * それぞれの動きは'north', 'east', 'south', 'west'のいずれかになります。
 * 
 * sample input:
 *  const moves = ['north', 'north', 'west', 'west', 'north', 'east','north']
 * sample output:
 *  [-1, 4]
 */

const finalPosition = (moves) => {
    // Code here!

    // Remember to return a value!
}
  