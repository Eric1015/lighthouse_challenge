/*
 * sphereVolume
 *  --> Given the radius, return the volume of sphere
 * coneVolume
 *  --> Given the radius and height, return the volume of cone
 * prismVolume
 *  --> Given the height, width, and depth, return the volume of prism
 * totalVolume
 *  --> Given an array that contains objects which hold information to calculate the volume, return the total volume of adding these different shapes
 * 
 * sample input:
 *  const largeSphere = {
      type: 'sphere',
      radius: 40
    }

    const smallSphere = {
      type: 'sphere',
      radius: 10
    }

    const cone = {
      type: 'cone',
      radius: 3,
      height: 5
    }

    const duck = [
      largeSphere,
      smallSphere,
      cone
    ]

    totalVolume(duck)

 * sample output:
 *  272318.257
 */

/*
 * sphereVolume
 *  --> 与えられた半径をもとに、球体の体積を求めなさい。
 * coneVolume
 *  --> 与えられた半径と高さをもとに、円錐の体積を求めなさい。
 * prismVolume
 *  --> 与えられた縦、横、高さをもとに、六面体の体積を求めなさい。
 * totalVolume
 *  --> 与えられた図形の情報を保持するオブジェクトの配列をもとに、すべての図形の総合の体積を求めなさい。
 * 
 * sample input:
 *  const largeSphere = {
      type: 'sphere',
      radius: 40
    }

    const smallSphere = {
      type: 'sphere',
      radius: 10
    }

    const cone = {
      type: 'cone',
      radius: 3,
      height: 5
    }

    const duck = [
      largeSphere,
      smallSphere,
      cone
    ]

    totalVolume(duck)

 * sample output:
 *  272318.257
 */

const PI = 3.14159 

const sphereVolume = function (radius) {
  // Code here

  // Remember to return a value
}

const coneVolume = function (radius, height) {
  // Code here

  // Remember to return a value
}

const prismVolume = function (height, width, depth) {
  // Code here

  // Remember to return a value
}

const totalVolume = function (solids) {
  // Code here

  // Remember to return a value;
}
