/*
 * Given arrays voter_signatures and voter_ids, check if they match in order or not.
 * If the length of each array is not the same, return false.
 * If there are mismatches between the two arrays, return "FRAUD!"
 * If there are no mismatches, return "All clear, we can count the votes!"
 * 
 * sample input:
 *  const voter_signatures = [
        'Bill Billiamson',
        'Kate Etak',
        'Brandon Brandonus',
        'Fake McFakerson',
        'Jane Janesford'
    ]

    const voter_ids = [
        'Bill Billiamson',
        'Kate Etak',
        'Brandon Brandonus',
        'Simon Simonson',
        'Jane Janesford'
    ]
 * sample output:
 *  FRAUD!
 */

/*
 * 与えられた投票者のサインと投票者のIDが順番通りにマッチするかどうか確認してください。
 * 2つの配列の長さが異なる場合、falseと返してください。
 * マッチしないものがあった場合、"FRAUD!"と返してください。
 * すべて問題なくマッチしたのなら、"All clear, we can count the votes!"と返してください。
 * 
 * sample input:
 *  const voter_signatures = [
        'Bill Billiamson',
        'Kate Etak',
        'Brandon Brandonus',
        'Fake McFakerson',
        'Jane Janesford'
    ]

    const voter_ids = [
        'Bill Billiamson',
        'Kate Etak',
        'Brandon Brandonus',
        'Simon Simonson',
        'Jane Janesford'
    ]
 * sample output:
 *  FRAUD!
 */

const voterTurnout = (voter_signatures, voter_ids) => {
    // Code here!

    // Remember to return a value!
}