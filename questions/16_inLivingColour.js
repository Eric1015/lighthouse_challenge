/*
 * Given an integer that represents the color code, return the two split complementary colors.
 * One of the split complementary color is color + 150
 * The other one is color + 210
 * The returned array will contain these two colors and the smaller one will be the first element of the returned array.
 * 
 * sample input:
 *  const color1 = 100
 * sample output:
 *  [250, 310]
 */

/*
 * 与えられた色を示す数字をもとにその補色を求めなさい。
 * 1つめの補色はcolor + 150
 * 2つ目の補色はcolor + 210
 * 戻り値の配列は2つの要素を持ち、一つ目は小さい方の色です。
 * 
 * sample input:
 *  const color1 = 100
 * sample output:
 *  [250, 310]
 */

const festivalColours = color1 => {
    // code here!

    // Remeber to return an array
}