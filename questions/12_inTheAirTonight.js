/*
 * Given an array of air condition samples and a threshold, return if the samples will be classified as 'Clean' or 'Polluted'.
 * If the ratio of 'dirty' in the samples is bigger than or equal to the threshold, it will be classified as 'Polluted', otherwise, it is 'Clean'.
 * 
 * sample input:
 *  const samples = ['clean', 'clean', 'dirty', 'clean', 'dirty', 'clean', 'clean', 'dirty', 'clean', 'dirty']
    const threshold = 0.3
 * sample output:
 *  Polluted
 */

/*
 * 与えられた空気のサンプルの状況と、基準をもとにこのサンプルの空気は汚れているのかきれいなのかを求めなさい。
 * もし、'dirty'の割合が基準以上でしたら、'Polluted'と、そうでなかったら、'Clean'と返してください。
 * 
 * sample input:
 *  const samples = ['clean', 'clean', 'dirty', 'clean', 'dirty', 'clean', 'clean', 'dirty', 'clean', 'dirty']
    const threshold = 0.3
 * sample output:
 *  Polluted
 */

const checkAir = function (samples, threshold) {
    // Code here!
    
    // Remember to return a value
}