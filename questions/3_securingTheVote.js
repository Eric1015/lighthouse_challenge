/*
 * Given the name and current votes record, increment the votes record according to the person who received the vote.
 * name will be one of 'Tim', 'Sally', 'Beth'
 * votes is an array that hold the current number of votes for each of 'Tim', 'Sally', 'Beth' in this order
 * 
 * sample input:
 *  const name = 'Sally';
    const votes = [
      0, // Tim
      2, // Sally
      1 // Beth
    ]
 * sample output:
 *  [
      0, // Tim
      3, // Sally
      1 // Beth
    ]
 */

/*
 * 与えられた名前と現在の投票記録をもとに、投票記録を更新してください。
 * 名前は'Tim', 'Sally', 'Beth'のいずれかになります。
 * votesは配列であり、前から順番に'Tim', 'Sally', 'Beth'の今の投票記録が入っています。
 * 
 * sample input:
 *  const name = 'Sally';
    const votes = [
      0, // Tim
      2, // Sally
      1 // Beth
    ]
 * sample output:
 *  [
      0, // Tim
      3, // Sally
      1 // Beth
    ]
 */

// Remember, the possible candidates are: Tim, Sally, and Beth (in that order)
const castVote = (name, votes) => {
    // Code here!

    // Remember to return a value!
}