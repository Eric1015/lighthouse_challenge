/*
 * Given an object buses which holds information about each bus, return an object that shows how much time it is needed for each bus to arrive.
 * Each object in buses will be in the following format (distance is the distance from you to that bus):
 *  {
        distance: 10,
        speed: 5
    }
 *
 * sample input:
 *  const buses = {
      pickadilly: {
          distance: 10,
          speed: 5
      },
      uptown: {
          distance: 13,
          speed: 10
      }
    }
 * sample output:
 *  {
      pickadilly: 2,
      uptown: 1.3
    }
 */

/*
 * 与えられたbusesというオブジェクトをもとにそれぞれのバスが到着するまでにかかる時間を求めなさい。
 * buses内のそれぞれのオブジェクトは下記のような構造になっています。(distanceは自分の今のところまでの距離になります。):
 *  {
        distance: 10,
        speed: 5
    }
 *
 * sample input:
 *  const buses = {
      pickadilly: {
          distance: 10,
          speed: 5
      },
      uptown: {
          distance: 13,
          speed: 10
      }
    }
 * sample output:
 *  {
      pickadilly: 2,
      uptown: 1.3
    }
 */

const busTimes = buses => {
    // Code here!
    
    // Remember to return an object!
}