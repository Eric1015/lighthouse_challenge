/*
 * Given number of participants and the distance of travel, return the total cost of the trip.
 * The base ticket price is $1.00
 * The passengers will be charged $0.25 for each 1 kilometer of travel.
 * If there are 30 or more than 30 passengers, there will be an additional $0.25
 * The returned value needs to be a string that is in format of '$0.00' where the decimal precision needs to be two digits.
 * 
 * Hint: use toFixed function for the answer so it will be two decimal precision
 * 
 * sample input:
 *  const numberOfPeople = 15
    const distanceTraveled = 10

 * sample output:
 *  $3.50
 */

/*
 * 与えられた参加者数と移動距離をもとに、合計のコストを計算しなさい。
 * チケットのベースは$1.00です。
 * 移動距離1キロメートルにつき$0.25の追加料金が適用されます。
 * 30人以上での参加の場合、$0.25の追加料金が適用されます。
 * 戻り値は'$0.00'のような少数第2位までの正確性を持った文字列の形でなければなりません。
 * 
 * Hint: toFixed関数を使えば、小数の正確性を好きな位に調整できます。
 * 
 * sample input:
 *  const numberOfPeople = 15
    const distanceTraveled = 10

 * sample output:
 *  $3.50
 */

const dynamicPricing = (numberOfPeople, distanceTraveled) => {
    // Code here!

    // Remeber to return a value
} 