/*
 * Given name and unregisteredVoters, register this person with the name, so that person should disappear from the unregisteredVoters.
 *
 * sample input:
 *  const name = 'Bradley';
 *  const unregisteredVoters = ['Jake', 'Alanna', 'Bradley', 'Stephanie'];
 * sample output:
 *  ['Jake', 'Alanna', 'Stephanie']
 */

/*
 * 与えられた名前と未登録者リストをもとにこの名前の人を登録し、未登録者リストから削除してください。
 *
 * sample input:
 *  const name = 'Bradley';
 *  const unregisteredVoters = ['Jake', 'Alanna', 'Bradley', 'Stephanie'];
 * sample output:
 *  ['Jake', 'Alanna', 'Stephanie']
 */

const registerToVote = (name, unregisteredVoters) => {
    // Code here!

    // Remember to return a value!
}  