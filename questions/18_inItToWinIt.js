/*
 * countTickets
 *  --> Given an array of tickets where each is a string represents its color, return an object that tells how many tickets there are for each color
 * 
 * sample input:
 *  const tickets = [
        'red',
        'red',
        'green',
        'blue',
        'green'
    ]
 * sample output:
 *  {
        red: 2,
        green: 2,
        blue: 1
    }
 *
 *
 * bestOdds
 *  --> Given an array of tickets where each is a string represents its color and raffle entries, return which color has the best odds
 *      The returned string will follow the format: "You have the best odds of winning the [COLOUR] raffle."
 * 
 * sample input:
 *  const tickets = [
        'red',
        'red',
        'green',
        'blue',
        'green'
    ]

    const raffleEntries = {
        red: 10,
        green: 30,
        blue: 15
    }
 * sample output:
 *  "You have the best odds of winning the red raffle."
 *//*
 * countTickets
 *  --> Given an array of tickets where each is a string represents its color, return an object that tells how many tickets there are for each color
 * 
 * sample input:
 *  const tickets = [
        'red',
        'red',
        'green',
        'blue',
        'green'
    ]
 * sample output:
 *  {
        red: 2,
        green: 2,
        blue: 1
    }
 *
 *
 * bestOdds
 *  --> Given an array of tickets where each is a string represents its color and raffle entries, return which color has the best odds
 *      The returned string will follow the format: "You have the best odds of winning the [COLOUR] raffle."
 * 
 * sample input:
 *  const tickets = [
        'red',
        'red',
        'green',
        'blue',
        'green'
    ]

    const raffleEntries = {
        red: 10,
        green: 30,
        blue: 15
    }
 * sample output:
 *  "You have the best odds of winning the red raffle."
 */

/*
 * countTickets
 *  --> 与えられたチケットの配列（各々が色を示す文字列）をもとにそれぞれの色が何回出てきたのかをオブジェクトに保存して戻り値として返しなさい。
 * 
 * sample input:
 *  const tickets = [
        'red',
        'red',
        'green',
        'blue',
        'green'
    ]
 * sample output:
 *  {
        red: 2,
        green: 2,
        blue: 1
    }
 *
 *
 * bestOdds
 *  --> 与えられたチケットの配列（各々が色を示す文字列）と色ごとの抽選チケット数をもとにどの色のチケットが最も抽選で当たる確率が高いのか求めなさい。
 *      戻り値の文字列は次のような形になります。: "You have the best odds of winning the [COLOUR] raffle."
 * 
 * sample input:
 *  const tickets = [
        'red',
        'red',
        'green',
        'blue',
        'green'
    ]

    const raffleEntries = {
        red: 10,
        green: 30,
        blue: 15
    }
 * sample output:
 *  "You have the best odds of winning the red raffle."
 */

const countTickets = (tickets) => {
    // Code here!

    // Remember to return an object
}
  
const bestOdds = (tickets, raffleEntries) => {
    // Code here!

    // Remember to return a value
}  